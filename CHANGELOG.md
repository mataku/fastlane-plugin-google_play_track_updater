# 0.2.0
Support `GOOGLE_APPLICATION_CREDENTIALS` environment variable for authentication (takes priority over `json_file_path` and `json_key_data`). This enables seamless integration with `google-github-actions/auth`.

# 0.1.0
Initial release.
